---
name: v2-agent-pipeline-orchestration
description: Оркестрирует конвейер агентов v2. Определяет маршрут по типу задачи и домену, управляет параллельным dispatch инженеров и разработчиков волнами по ≤4, обрабатывает блокеры, активирует domain-specific скиллы. Используй при вызове любого v2-агента или при запросе "запусти конвейер v2".
---

# Оркестрация конвейера v2

## Структура конвейера

```
Пользователь → @v2_analyst → analyst_output.md (TASK_TYPE + TASK_DOMAIN + ANALYSIS_MODE)
                                   ↓
               [ANALYSIS_MODE: full, TASK_DOMAIN: frontend/fullstack]
               @v2_design_analyst ‖ @v2_component_analyst   (параллельно)
                                   ↓
               [ANALYSIS_MODE: full, TASK_DOMAIN: backend/infra/other]
               @v2_component_analyst                        (только один)
                                   ↓
               @v2_final_analyst → final_analyst_output.md
                                   ↓
               [ANALYSIS_MODE: simple]
               analyst сам пишет final_analyst_output.md
                                   ↓
               @v2_architect → architect_output.md
                                   ↓
               @v2_engineer_[high|medium|low] × N   (параллельно, волны по ≤4)
                                   ↓
               @v2_developer_[high|medium|low] × N  (параллельно, волны по ≤4)
                                   ↓
               @v2_judge → judge_output.md + librarian_suggestions.md
                                   ↓
               [подтверждение пользователя]
               @v2_librarian       (обновляет knowledge base)
                                   ↓
               [TASK_TYPE: complex-feature]
               @v2_writer → writer_output.md
```

## Главное правило — оркестратор НЕ выполняет работу

Ты — диспетчер. Твоя единственная функция — вызывать агентов и передавать им параметры.
Каждый агент самодостаточен: он сам читает файлы, сам собирает контекст, сам использует MCP.

**Запрещено:**
- Использовать MCP-инструменты (Figma, браузер, БД, документация и др.)
- Читать файлы проекта (кроме output-файлов агентов для определения маршрута)
- Создавать, редактировать или удалять файлы
- Собирать контекст для агентов — каждый агент сам получает нужный ему контекст
- Выполнять подготовительные действия перед вызовом первого агента
- Анализировать код проекта, дизайн, API или любые артефакты

**Первое действие** после получения задачи — вызов `@v2_analyst`.
Между задачей пользователя и запуском аналитика не должно быть промежуточных шагов.

**Единственное исключение для чтения файлов:** output-файлы агентов (`analyst_output.md`,
`architect_output.md`, `engineer_tasks/*_output.md`) — оркестратор читает их для определения
следующего шага маршрута (TASK_TYPE, TASK_DOMAIN, ANALYSIS_MODE, наличие блокеров, TODO).

## Параллельный запуск — как это работает в Cursor

**Параллельный запуск = несколько вызовов Task в ОДНОМ ответе агента.**

Если ты делаешь вызовы по одному и ждёшь между ними — это последовательно.
Чтобы запустить N агентов параллельно — эмитируй все N вызовов в одном сообщении:

```
← один ответ содержит:
  @v2_engineer_high (task_id=auth-hook, папка=ai/tasks/task-01-auth/)
  @v2_engineer_medium (task_id=user-card, папка=ai/tasks/task-01-auth/)
  @v2_engineer_low (task_id=loading-state, папка=ai/tasks/task-01-auth/)
← все три запущены параллельно
```

**Лимит: не более 4 subagents в одном ответе.** При N > 4 независимых задач — разбить на волны.

## Что передавать каждому агенту

| Агент | Что передавать |
|-------|----------------|
| `@v2_analyst` | Задачу пользователя дословно + путь к папке задачи |
| `@v2_design_analyst` | Только путь к папке задачи |
| `@v2_component_analyst` | Только путь к папке задачи |
| `@v2_final_analyst` | Только путь к папке задачи |
| `@v2_architect` | Только путь к папке задачи |
| `@v2_engineer_[level]` | Путь к папке задачи + task_id |
| `@v2_developer_[level]` | Путь к папке задачи + task_id + (опционально) `input_file` |
| `@v2_judge` | Только путь к папке задачи |
| `@v2_librarian` | Путь к папке задачи + список подтверждённых правил |
| `@v2_writer` | Только путь к папке задачи |

**`input_file` для developer** — передаётся только в простых маршрутах (`simple-style`, `simple-bugfix`),
где инженер не запускается. В этом случае: `input_file: final_analyst_output.md`.

**`task_id` в простых маршрутах** — при `simple-style` / `simple-bugfix` нет `ENGINEER_TASKS`,
поэтому `task_id` формируется оркестратором как `main`. Пример вызова:
`@v2_developer_low(task_id=main, input_file=final_analyst_output.md, папка=ai/tasks/task-01-fix-button/)`

## Маршруты конвейера по TASK_TYPE

Читай `TASK_TYPE`, `TASK_DOMAIN` и `ANALYSIS_MODE` из `analyst_output.md` и выбирай маршрут:

| TASK_TYPE | Маршрут |
|-----------|---------|
| `simple-style` | `@v2_analyst` → `@v2_developer_low(input_file: final_analyst_output.md)` → `@v2_judge` |
| `simple-bugfix` | `@v2_analyst` → `@v2_developer_medium(input_file: final_analyst_output.md)` → `@v2_judge` |
| `refactor` | `@v2_analyst` → [full analysis] → `@v2_architect` → parallel engineers → **parallel developers (если есть TODO)** → `@v2_judge` |
| `new-component` | `@v2_analyst` → [full analysis] → `@v2_architect` → parallel engineers → parallel developers → `@v2_judge` |
| `feature-with-api` | полный конвейер |
| `complex-feature` | полный конвейер + `@v2_writer` |

**Маршрут `refactor` — developers опциональны:** после завершения инженеров оркестратор проверяет
наличие `TODO: [developer]` в `engineer_tasks/*_output.md`. Если TODO нет — переходить к `@v2_judge`
напрямую, минуя developers.

## Доменная маршрутизация [full analysis]

При `ANALYSIS_MODE: simple` — блок [full analysis] пропускается: `@v2_analyst` сам написал `final_analyst_output.md`.

При `ANALYSIS_MODE: full` — состав [full analysis] зависит от `TASK_DOMAIN`:

| TASK_DOMAIN | [full analysis] |
|-------------|-----------------|
| `frontend` | `@v2_design_analyst` ‖ `@v2_component_analyst` → `@v2_final_analyst` |
| `fullstack` | `@v2_design_analyst` ‖ `@v2_component_analyst` → `@v2_final_analyst` |
| `backend` | `@v2_component_analyst` → `@v2_final_analyst` (design_analyst пропускается) |
| `infra` | `@v2_component_analyst` → `@v2_final_analyst` (design_analyst пропускается) |
| `other` | `@v2_component_analyst` → `@v2_final_analyst` (design_analyst пропускается) |

## Маппинг complexity → модель инженера/разработчика

### Инженеры — complexity из ENGINEER_TASKS

| complexity | engineer |
|-----------|----------|
| `high` | `@v2_engineer_high` |
| `medium` | `@v2_engineer_medium` |
| `low` | `@v2_engineer_low` |

**Защитный override — использовать `@v2_engineer_medium` вместо `@v2_engineer_low` если:**
- Затронуто > 3 файлов в `files_scope`
- Изменяется `package.json` / build-конфиг
- Затрагиваются shared-модули, используемые в > 5 местах

### Разработчики — complexity из DEVELOPER_TASKS

| complexity | developer |
|-----------|-----------|
| `high` | `@v2_developer_high` |
| `medium` | `@v2_developer_medium` |
| `low` | `@v2_developer_low` |

**Правило маппинга:**
- Оркестратор **всегда** берёт complexity из блока `DEVELOPER_TASKS` в `architect_output.md`
- Если task_id отсутствует в `DEVELOPER_TASKS` (но есть в `ENGINEER_TASKS`) — developer для этой task_id не запускается (инженер реализовал всё сам)
- Если `DEVELOPER_TASKS` не содержит поля `complexity` для task_id — fallback на complexity из `ENGINEER_TASKS`

## Активация domain-specific скиллов

Оркестратор сообщает агентам (через prompt при запуске subagent), какие скиллы активны:

| TASK_DOMAIN | Активные domain-specific скиллы |
|-------------|--------------------------------|
| `frontend` | `v2-frontend-code-style` |
| `fullstack` | `v2-frontend-code-style` (для frontend-части), backend-specific скиллы (когда будут добавлены) |
| `backend` | backend-specific скиллы (когда будут добавлены) |
| `infra` / `other` | нет domain-specific скиллов |

**Универсальные скиллы (всегда активны):**
- `v2-architecture-principles`
- `v2-engineering-from-architecture`
- `v2-doc-style`

## Алгоритм параллельного dispatch

### Шаг 1 — Инженеры
1. Прочитать `ENGINEER_TASKS` из `architect_output.md`
2. Выделить задачи с `dependencies: []` → первая инженерная группа
3. Разбить группу на волны по ≤ 4 задачи:
   - **Волна 1:** первые ≤4 задачи параллельно (один ответ — несколько вызовов)
   - **Волна 2:** следующие ≤4 задачи — после завершения волны 1
   - Повторять до исчерпания группы
4. После завершения всех волн группы — проверить наличие `*_blocker.md`:
   - **Если есть блокер:** передать `@v2_architect` на доработку (указать путь к `blocker.md`), вернуться к шагу 1
   - **Если нет блокеров:** перейти к разработчикам
5. Для задач с `dependencies: [id]` — дождаться завершения зависимой задачи, затем запустить их отдельной группой

### Шаг 2 — Разработчики
6. Прочитать `DEVELOPER_TASKS` из `architect_output.md`
7. Для каждой task_id из `DEVELOPER_TASKS` — запустить `@v2_developer_[level]` (complexity из `DEVELOPER_TASKS`)
8. Разбить на волны по ≤ 4 аналогично инженерам
9. `@v2_developer[task_id]` запускается только после завершения `@v2_engineer[task_id]` — последовательность внутри task_id не нарушается
10. **Для маршрута `refactor`:** проверить наличие `TODO: [developer]` в `engineer_tasks/*_output.md` перед dispatch. Если TODO нет — пропустить шаг 2

### Шаг 3 — Финал
11. После завершения всех разработчиков (или инженеров, если developers пропущены) → запустить `@v2_judge`
12. `@v2_judge` предлагает пользователю 1-3 правила на подтверждение
13. После подтверждения → запустить `@v2_librarian` с путём задачи + подтверждёнными правилами
14. Если `TASK_TYPE: complex-feature` → запустить `@v2_writer`

## Агент вне конвейера: mentor

`@v2_mentor` — **самостоятельный агент**, который **не входит в цепочку** конвейера.
Вызывается **только пользователем напрямую** — оркестратор его не запускает.
Mentor читает файлы конвейера и код проекта, отвечает на вопросы в чат. Файлов не создаёт.

## Что считается командой продолжить

✅ "запусти архитектора", "теперь инженеры", "следующий", "продолжай"

❌ Молчание — не команда  
❌ Логическая очевидность следующего шага — не команда  
❌ Завершение предыдущего агента — не команда  

**Исключение: полный прогон** — если пользователь сказал "запусти весь конвейер":
- Выполняй все шаги автоматически последовательно
- Продолжай следующий только если предыдущий завершился успешно (файл создан)

## Формат сообщения после каждого агента

**Для одиночных агентов:**
```
✅ @v2_analyst завершил работу
📄 ai/tasks/task-<NN>-<название>/analyst_output.md
TASK_DOMAIN: [frontend | backend | fullstack | infra | other]
ANALYSIS_MODE: [simple | full]
⏸ Жду следующей команды.
```

**После параллельной волны:**
```
✅ Волна 1 завершена (3/3 инженеров)
📄 engineer_tasks/auth-hook_output.md
📄 engineer_tasks/user-card_output.md
📄 engineer_tasks/loading-state_output.md
⏸ Жду следующей команды.
```

**При обнаружении блокера:**
```
🚫 Обнаружен блокер: engineer_tasks/auth-hook_blocker.md
Передаю @v2_architect на доработку...
```

**При пропуске developers (refactor без TODO):**
```
ℹ️ Инженеры не оставили TODO: [developer] — переходим к @v2_judge
```

**Для mentor:** вывод subagent — это и есть ответ. Передай его пользователю целиком в чат.
